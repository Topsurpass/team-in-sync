import Provider from "@/providers";

function App() {
	return (
		<Provider>
			<div className="flex h-screen items-center justify-center border">
				<h1 className="animate-bounce text-4xl font-bold">TEAM IN SYNC</h1>
			</div>
		</Provider>
	);
}

export default App;
